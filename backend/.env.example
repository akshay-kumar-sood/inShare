# Base URL of your app (on Render: https://your-app-name.onrender.com)
APP_BASE_URL=http://localhost:3000

# MongoDB connection string (use MongoDB Atlas for production)
MONGO_CONNECTION_URL=mongodb://localhost:27017/inshare

# Gmail: full email + App Password from https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
MAIL_USER=
MAIL_PASSWORD=

# Optional: comma-separated origins for CORS (leave empty for *)
ALLOWED_CLIENTS=
